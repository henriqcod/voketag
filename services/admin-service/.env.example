# Admin Service Environment Variables

# Application (PORT=8082 matches frontend Admin next.config.js / NEXT_PUBLIC_ADMIN_API)
ENV=development
PORT=8082
LOG_LEVEL=INFO

# Database (shared with Factory Service)
DATABASE_URL=postgresql+asyncpg://voketag:password@postgres:5432/voketag

# Redis (shared)
REDIS_URL=redis://:changeme@redis:6379/0

# JWT Auth (shared with Factory Service)
JWT_SECRET=your-secret-key-change-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,https://app.voketag.com
